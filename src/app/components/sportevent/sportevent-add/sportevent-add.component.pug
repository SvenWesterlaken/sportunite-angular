.form-container
    form([formGroup]="getForm()", (ngSubmit)="onSubmit()")
        app-stepper([linear]="true")

            app-step([stepControl]="getForm().get('name')")
                .big-form-title-container
                  h3.big-form-header Wat is de naam van de sportactiviteit?

                .big-form-input-container
                    mat-form-field.big-form-field(floatLabel="never")
                        input#name(matInput type="text", formControlName="name", placeholder="Vul een naam in")
                        mat-error(*ngIf="getForm().get('name').hasError('required')") Dit veld is verplicht

                .big-form-footer-container
                  span.step-counter {{stepper.selectedIndex + 1}}/{{stepper._steps.length}}
                  button.btn.btn-large(appStepperNext, [disabled]="getForm().get('name').invalid", type='button') Volgende stap

            app-step([stepControl]="getForm().get('sport')")
                .big-form-title-container
                  h3.big-form-header Welke sport hoort bij deze activiteit?

                .big-form-input-container
                    mat-form-field.big-form-field(floatLabel="never")
                        mat-select#sport(matInput type="text", formControlName="sport", placeholder="Vul een sport in")
                            mat-option(*ngFor="let sport of sports", [value]="sport")
                                p {{sport.name}}
                        mat-error(*ngIf="getForm().get('sport').hasError('required')") Selecteer een sport om verder te gaan
                .big-form-footer-container
                  span.step-counter {{stepper.selectedIndex + 1}}/{{stepper._steps.length}}
                  button.btn.btn-large(appStepperNext, [disabled]="getForm().get('sport').invalid", type='button') Volgende stap

            app-step(formGroupName="event-date", [stepControl]="getForm().get('event-date')")
                .big-form-title-container
                  h3.big-form-header Wanneer vindt de sportactiviteit plaats?

                .big-form-input-container
                    mat-form-field.big-form-field(floatLabel="never")
                        input#startTime(matInput type="time", formControlName="startTime", placeholder="Begintijd", [(ngModel)]="startTime", (change)="revalidateDate()", (input)="revalidateDate()")
                        mat-error(*ngIf="getForm().get('event-date.startTime').hasError('required')") Dit veld is verplicht
                    span Tot
                    mat-form-field.big-form-field(floatLabel="never")
                        input#endTime(matInput type="time", formControlName="endTime", placeholder="Eindtijd", [(ngModel)]="endTime", app-form-valid [minimumValue]="startTime")
                        mat-error(*ngIf="getForm().get('event-date.endTime').hasError('required')") Dit veld is verplicht
                        mat-error(*ngIf="getForm().get('event-date.endTime').hasError('minimumValue')") Eindtijd mag niet voor begintijd liggen
                    span Op
                    mat-form-field.big-form-field(floatLabel="never")
                        input(matInput, [min]="tomorrow", [matDatepicker]="datePicker", placeholder="Datum", formControlName="date", type="text")
                        mat-datepicker-toggle(matSuffix, [for]="datePicker")
                        mat-datepicker(#datePicker)
                        mat-error(*ngIf="getForm().get('event-date.date').hasError('required')") Dit veld is verplicht

                .big-form-footer-container
                    span.step-counter {{stepper.selectedIndex + 1}}/{{stepper._steps.length}}
                    button.btn.btn-large(appStepperNext, [disabled]="getForm().get('event-date').invalid", type='button') Volgende stap

            app-step(formGroupName="location", [stepControl]="getForm().get('location')")
                .big-form-title-container
                    h3.big-form-header Waar zal de sportactiviteit plaatsvinden?

                .big-form-input-container

                    mat-form-field.big-form-field(floatLabel="never")
                      input#city(matInput type="text", formControlName="city", placeholder="Vul een stad in", (change)="getBuildings(eventForm.get('location.city').value)")
                      mat-error(*ngIf="getForm().get('location.city').hasError('required')") Dit veld is verplicht
                      mat-error(*ngIf="getForm().get('location.city').hasError('notFound')") Er zijn geen sportcomplexen gevonden in deze stad

                    mat-form-field.big-form-field(floatLabel="never")
                      mat-select#building(matInput type="text", formControlName="building", placeholder="Vul een gebouw in", (change)="getHalls(eventForm.get('location.building').value, eventForm.get('sport').value)")
                        mat-option(*ngFor="let building of resultBuildings", [value]="building")
                          p {{ building.name }}
                      mat-error(*ngIf="getForm().get('location.building').hasError('required')") Voer een sportcomplex in om verder te gaan
                      mat-error(*ngIf="getForm().get('location.building').hasError('notFound')") Er zijn geen sportzalen gevonden die voldoen aan jouw voorkeuren

                    mat-form-field.big-form-field(floatLabel="never")
                      mat-select#location(matInput type="text", formControlName="hall", placeholder="Vul een sportlocatie in")
                        mat-option(*ngFor="let hall of resultHalls", [value]="hall")
                          p {{hall.name}}
                      mat-error(*ngIf="getForm().get('location.hall').hasError('required')") Dit veld is verplicht



                .big-form-footer-container
                    span.step-counter {{stepper.selectedIndex + 1}}/{{stepper._steps.length}}
                    button.btn.btn-large(appStepperNext, [disabled]="getForm().get('location').invalid", type='button') Volgende stap

            app-step(formGroupName="event-attendees", [stepControl]="getForm().get('event-attendees')")
                .big-form-title-container
                  h3.big-form-header Hoeveel mensen wil je laten deelnemen?
                  span.big-form-subheader(style="font-style: italic" ) (inclusief jezelf)
                .big-form-input-container
                    mat-form-field.big-form-field(floatLabel="never")
                        input#minAttendees(matInput type="number", style="text-align:center", min=2, formControlName="minAttendees", placeholder="Minimum", [(ngModel)]="inputValue", (change)="revalidateAttendees()", (input)="revalidateAttendees()")
                        mat-error(*ngIf="getForm().get('event-attendees.minAttendees').hasError('required')") Dit veld is verplicht
                        mat-error(*ngIf="getForm().get('event-attendees.minAttendees').hasError('min')") Voer minimaal 2 deelnemers in
                    span tot
                    mat-form-field.big-form-field(floatLabel="never")
                        input#maxAttendees(matInput type="number", style="text-align:center", min=2,  formControlName="maxAttendees", placeholder="Maximum", app-form-valid [minimumValue]="inputValue")
                        mat-error(*ngIf="getForm().get('event-attendees.maxAttendees').hasError('required')") Dit veld is verplicht
                        mat-error(*ngIf="getForm().get('event-attendees.maxAttendees').hasError('min')") Voer minimaal 2 deelnemers in
                        mat-error(*ngIf="getForm().get('event-attendees.maxAttendees').hasError('minimumValue')") Het minimale aantal deelnemers kan niet meer zijn dan het maximum
                    span personen

                .big-form-footer-container
                  span.step-counter {{stepper.selectedIndex + 1}}/{{stepper._steps.length}}
                  button.btn.btn-large(appStepperNext, [disabled]="getForm().get('event-attendees').invalid", type='button') Volgende stap


            app-step([stepControl]="getForm().get('description')")
                .big-form-title-container
                  h3.big-form-header Wil je nog meer informatie geven over de sportactiviteit?

                .big-form-input-container
                    mat-form-field.big-form-field(floatLabel="never")
                        textarea#description(matInput type="text", formControlName="description", placeholder="Vul een beschrijving in")
                        mat-error(*ngIf="getForm().get('description').hasError('minlength')") Moet uit minimaal 50 tekens bestaan
                        mat-error(*ngIf="getForm().get('description').hasError('maxlength')") Mag uit maximaal 500 tekens bestaan
                        mat-error(*ngIf="getForm().get('description').hasError('required')") Dit veld is verplicht

                .big-form-footer-container
                    span.step-counter {{stepper.selectedIndex + 1}}/{{stepper._steps.length}}
                    button.btn.btn-large(appStepperNext, [disabled]="getForm().get('description').invalid", type='button') Bekijk resultaat

            app-step
                mat-card
                    mat-card-header
                        mat-card-title
                          h3 Overzicht
                        mat-card-subtitle Er is de mogelijkheid om dingen aan te passen door terug te gaan
                    mat-card-content.summary
                        h4 {{ eventForm.value.name }}
                        p Om bezig te zijn met
                          span.card  {{ eventForm.value.sport.name }}
                        p Op #[span.card {{ eventForm.get('event-date.date').value | date: 'longDate' }}] van #[span.card {{ eventForm.get('event-date.startTime').value }}] tot #[span.card {{ eventForm.get('event-date.endTime').value }}]
                        p Bij #[span.card {{ eventForm.get('location.building').value.name }}] in  #[span.card {{ eventForm.get('location.hall').value.name }}]op  #[span.card {{ streetName }} {{ houseNumber }} ] te #[span.card {{ eventForm.get('location.city').value }}]
                        p Met #[span.card {{ eventForm.get('event-attendees.minAttendees').value }}] tot #[span.card {{ eventForm.get('event-attendees.maxAttendees').value }}] personen


                    mat-card-content.footer
                        h4 Extra informatie
                        p {{ eventForm.value.description }}



                .big-form-footer-container
                  span.step-counter {{stepper.selectedIndex + 1}}/{{stepper._steps.length}}
                  button.btn.btn-large(type="submit", [disabled]="!getForm().valid") Evenement aanmaken
