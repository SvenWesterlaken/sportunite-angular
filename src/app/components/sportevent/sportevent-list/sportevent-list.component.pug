.wrapper

  .add-container
    .row.add
      button.btn((click)="onCreateNewEvent()")
        mat-icon(svgIcon="plus")
      label Nieuw evenement

  .event-container

    .search-container
      input(#titleInput type="text",  (keyup.enter)="onFilter()", (change)="onFilter()")
      mat-icon(svgIcon="magnify", (click)="onFilter()" )
      

    app-sportevent-list-item( *ngFor="let event of resultEvents; let i = index", [event] = "event", [index] = "event.sportEventId" )
    p(*ngIf="!resultEvents") Geen evenementen om weer te geven
    p(*ngIf="resultEvents?.length === 0") Er zijn geen evenementen gevonden


  .filter-container
      button.btn((click)="onToggle()")
        mat-icon(svgIcon="filter-variant")

      app-sportevent-list-filter(*ngIf="toggleOpen", (handleSelectedSports)="getFilterData($event)")


